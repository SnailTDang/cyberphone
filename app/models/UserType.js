export default class UserType {
    constructor(id, account, name, cart, notica, token) {
        this.id = id;
        this.account = account;
        this.name = name;
        this.cart = cart;
        this.notica = notica;
        this.token = token;
    }
}