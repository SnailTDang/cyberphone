export default class UserType {
    constructor(id, account, name, password, cart, notica) {
        this.id = id;
        this.account = account;
        this.name = name;
        this.password = password;
        this.cart = cart;
        this.notica = notica
    }
}